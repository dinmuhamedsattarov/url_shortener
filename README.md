# Django URL Shortener

A simple URL shortener web app built with Django.

## Features

- Shorten long URLs
- Clean, centered interface
- Prevents duplicate short URLs
- Redirects to original URL

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/dinmuhamedsattarov/url_shortener
